const biggestNumber = (a,b,c)=>{

        //the list of numbers 
          var a;
          var b;
          var c;
      
      //the function conditions
          if(a>b && a>c){
            return a;
          }
            else if(b>a && b>c){
            return b;
          }
      
          //if the third letter does not meet the terms of the first two 
          //if function he has to meet the terms for the third function
            else
            return c;
          }
           