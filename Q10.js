function LongestWord(arr) {
    
        //Setting the sum to zero 
        let sum = 0;
    
        //for loop to 
        for(let a of arr)
    
         //the sum plus the 'arr' argument 
        sum +=a;
        return sum;
    ;}